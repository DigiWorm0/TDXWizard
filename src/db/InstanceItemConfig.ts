export default interface InstanceItemConfig {
    id: string | number;
    alias: string;
    hide?: boolean;
}

// Ticket Types (Reassignment)
// Apps (Search)
// Groups (Assignment)

// Regex for Assets